<!DOCTYPE html>
<html>
<head>
  <title>My Library</title>
</head>
<body>
  <h1>Books In My Library</h1>
  <ul>

  <% for (let i = 0; i < library.length; i++) { %>
    <li> 

    <a href="/library/<%=i%>"><%=library[i].title; %></a>, written by <%=library[i].author;%>. <br> <%if(library[i].currentlyReading === true){%>
    You are currently reading this book.
    <%} else {%>
    <%} %>
    <%if(library[i].read === true){%>
    You have previously read this book.
    <%} else {%>
    You have not yet read this book.
    <%} %>

    <form method="POST" action="/library/<% library[i].id%>?_method=DELETE">
      <input type="submit" name="DELETE">
    </form>
   
    <a href="/library/<%=library[i].id%>/edit">EDIT</a>
    
<%} %>

    </li>
  </ul>
<nav>
  <a href="/library/new">Add a new book</a>
</nav>
</body>
</html>